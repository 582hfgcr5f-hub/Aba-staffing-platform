export const INTERVIEW_STATUSES = ["Scheduled", "Completed", "Follow Up", "Offer", "Hired", "Declined", "No Show"] as const;
export const EVALUATION_GRADES = ["A+", "A", "A-", "B+", "B", "B-", "C+", "C", "C-", "D", "F"] as const;

export type InterviewStatus = (typeof INTERVIEW_STATUSES)[number];
export type EvaluationGrade = (typeof EVALUATION_GRADES)[number];

export type InterviewRecord = {
  id: string;
  candidateName: string;
  phone: string;
  email: string;
  city: string;
  state: string;
  zip: string;
  scheduledAt: string;
  status: InterviewStatus;
  experience: string;
  desiredPay: string;
  employmentType: string;
  availability: string;
  availableDays: string[];
  preferredStartTime: string;
  preferredEndTime: string;
  travelRadiusMinutes?: number;
  centralReachExperience: string;
  preferredContact: string;
  rating: string;
  recruiterNotes: string;
  yearsAba?: number;
  yearsRbt?: number;
  inHomeExperience?: boolean;
  clinicExperience?: boolean;
  severeBehaviorsExperience?: boolean;
  preferredAgeGroup: string;
  earliestStartDate: string;
  certifications: string[];
  certificationOther: string;
  skills: string[];
  skillOther: string;
  communicationScore?: number;
  professionalismScore?: number;
  abaExperienceScore?: number;
  scheduleFlexibilityScore?: number;
  overallRecommendationScore?: number;
  communicationGrade?: EvaluationGrade;
  professionalismGrade?: EvaluationGrade;
  abaExperienceGrade?: EvaluationGrade;
  scheduleFlexibilityGrade?: EvaluationGrade;
  overallRecommendationGrade?: EvaluationGrade;
  offerPay: string;
  offerNotes: string;
  offerDate: string;
  interviewOutcome: "" | "Strong Hire" | "Hire" | "Maybe" | "Hold" | "Decline";
  caseRecommendation: string;
  caseRecommendationId?: string;
  resumePath: string;
  resumeName: string;
  resumeMimeType: string;
  backgroundCheckSubmitted: boolean;
  backgroundCleared: boolean;
  drugScreen: boolean;
  cprVerified: boolean;
  rbtLicenseVerified: boolean;
  technicianId?: string;
};

export type InterviewEvent = {
  id: string;
  interviewId: string;
  eventType: string;
  detail: string;
  createdAt: string;
};